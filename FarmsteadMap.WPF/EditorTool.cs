namespace FarmsteadMap.WPF
{
    public enum EditorTool
    {
        Select,      // Виділення та переміщення
        Pan,         // Переміщення полотна
        PlaceAsset,  // Розміщення об'єктів
        DrawArea,    // Малювання замкнутих зон (полігони)
        DrawLine,    // Малювання ліній/пензлик
        Ruler        // Лінійка
    }
}