<UserControl x:Class="FarmsteadMap.WPF.MyMapsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FarmsteadMap.WPF"
             xmlns:converters="clr-namespace:FarmsteadMap.WPF.Converters" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000"
             Background="WhiteSmoke"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <converters:MapJsonToImageConverter x:Key="MapJsonConverter"/>

        <Style x:Key="ContextMenuButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#99FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock Text="···" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Margin="0,-6,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MapTile" TargetType="Border">
            <Setter Property="Width" Value="240"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="10" Color="#33000000" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="MapItemTemplate">
            <Border Style="{StaticResource MapTile}" Cursor="Hand" MouseLeftButtonUp="OpenMap_Click" Tag="{Binding Id}">
                <Grid>
                    <Border CornerRadius="12" ClipToBounds="True">
                        <Image Source="{Binding MapJson, Converter={StaticResource MapJsonConverter}}" 
                               Stretch="UniformToFill" 
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    </Border>

                    <Border VerticalAlignment="Bottom" Background="#CCFFFFFF" CornerRadius="0,0,12,12" Padding="15,10">
                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#222"/>
                    </Border>

                    <Button Style="{StaticResource ContextMenuButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Click="ContextMenuButton_Click">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Перейменувати" Click="RenameMenuItem_Click"/>
                                <MenuItem Header="Видалити" Click="DeleteMenuItem_Click"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="20">
                <ItemsControl x:Name="MapItemsControl" ItemTemplate="{StaticResource MapItemTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <Border Style="{StaticResource MapTile}" Background="#F5F5F5" Cursor="Hand" MouseLeftButtonUp="AddMapTile_MouseUp">
                    <Grid>
                        <Rectangle RadiusX="12" RadiusY="12" Stroke="#BBB" StrokeThickness="2" StrokeDashArray="5 3" Margin="0"/>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Path Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
                                  Fill="#BBB" Width="40" Height="40" Stretch="Uniform"/>
                            <TextBlock Text="Створити мапу" Margin="0,10,0,0" Foreground="#999" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </WrapPanel>
        </ScrollViewer>

        <Grid x:Name="AddMapPopup" Background="#99000000" Visibility="Collapsed">
            <Border Background="White" CornerRadius="12" Padding="25" VerticalAlignment="Center" HorizontalAlignment="Center" Width="420">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="25" Opacity="0.4"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock x:Name="PopupTitle" Text="Створити нову мапу" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                    <TextBlock Text="Назва ділянки:" Margin="0,10,0,8" Foreground="#555" FontSize="14"/>
                    <TextBox x:Name="NewMapNameTextBox" FontSize="16" Padding="8" BorderBrush="#DDD" BorderThickness="1"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <Button x:Name="CancelMapButton" Content="Скасувати" Padding="15,8" Margin="10,0" Background="Transparent" BorderThickness="0" Foreground="#555" Click="CancelMapButton_Click"/>
                        <Button x:Name="CreateMapButton" Content="Створити" FontWeight="Bold" Padding="20,8" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="CreateMapButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>