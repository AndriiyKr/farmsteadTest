<UserControl x:Class="FarmsteadMap.WPF.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FarmsteadMap.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="#F0F2F5">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#222222"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#555555"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DDE1E6"/>
        <SolidColorBrush x:Key="BackgroundLightBrush" Color="White"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#D9534F"/>

        <Style x:Key="SettingsNavMenu" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,12"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="Transparent" BorderThickness="2,0,0,0" BorderBrush="Transparent" CornerRadius="4" SnapsToDevicePixels="True" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                            <Setter TargetName="Bd" Property="Background" Value="#11007ACC"/>
                                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#08000000"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsCard" TargetType="Grid">
            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="10" Color="Black" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        <Style x:Key="SectionDescription" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        <Style x:Key="SettingsFormField" TargetType="Grid">
            <Setter Property="Margin" Value="0,15"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>
        <Style x:Key="FieldDescription" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="#FDFDFD"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="5" Color="#007ACC" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsPasswordBox" TargetType="PasswordBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="#FDFDFD"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,10,10,8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="5" Color="#007ACC" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C9302C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CardFooter" TargetType="Border">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="25,15"/>
        </Style>


        <Style x:Key="ModernSettingsTabItem" TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="False"/>
                        <Condition Property="IsMouseOver" Value="True"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernSettingsTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernSettingsTabItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                <TabPanel x:Name="HeaderPanel"
                                          IsItemsHost="True"
                                          Margin="0,0,0,-1" Background="Transparent"
                                          KeyboardNavigation.TabIndex="1"/>
                            </Border>
                            <Border x:Name="ContentPanel"
                                    Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    KeyboardNavigation.DirectionalNavigation="Contained"
                                    KeyboardNavigation.TabIndex="2"
                                    KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  Margin="{TemplateBinding Padding}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="SettingsNav" Grid.Column="0" 
                 Style="{StaticResource SettingsNavMenu}"
                 SelectionChanged="SettingsNav_SelectionChanged">

            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <Path Data="M12,12C14.21,12 16,10.21 16,8C16,5.79 14.21,4 12,4C9.79,4 8,5.79 8,8C8,10.21 9.79,12 12,12M12,14C9.33,14 4,15.33 4,18V20H20V18C20,15.33 14.67,14 12,14Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="25" Height="25" Stretch="Fill"/>
                    <TextBlock Text="Профіль" Margin="15,0,0,0" VerticalAlignment="Center" FontSize="15"/>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <Path Data="M12,1L3,5V11C3,16.55 6.84,21.74 12,23C17.16,21.74 21,16.55 21,11V5L12,1M12,11H19V11C19,15.5 16,19.5 12,20.92C8,19.5 5,15.5 5,11V6.3L12,3.2V11Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="25" Height="25" Stretch="Fill"/>
                    <TextBlock Text="Безпека" Margin="15,0,0,0" VerticalAlignment="Center" FontSize="15"/>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <Path Data="M19,19H5V5H12V3H5C3.89,3 3,3.9 3,5V19C3,20.1 3.9,21 5,21H19C20.1,21 21,20.1 21,19V12H19V19M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3H14Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="25" Height="25" Stretch="Fill"/>
                    <TextBlock Text="Додатково" Margin="15,0,0,0" VerticalAlignment="Center" FontSize="15"/>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem Foreground="{StaticResource DangerBrush}">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M10,13C12.21,13 14,11.21 14,9C14,6.79 12.21,5 10,5C7.79,5 6,6.79 6,9C6,11.21 7.79,13 10,13M10,15C6.67,15 0,16.67 0,20V23H20V20C20,16.67 13.33,15 10,15M23,18H17V20H23V18Z" 
                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Width="25" Height="25" Stretch="Fill"/>
                    <TextBlock Text="Видалення профілю" Margin="15,0,0,0" VerticalAlignment="Center" FontSize="15"/>
                </StackPanel>
            </ListBoxItem>
        </ListBox>

        <TabControl x:Name="ContentTabControl" Grid.Column="1" 
                    BorderThickness="0" Background="Transparent" Padding="0">

            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
                    <StackPanel MaxWidth="900">
                        <Grid Style="{StaticResource SettingsCard}">
                            <StackPanel>
                                <StackPanel Margin="25,25,25,0">
                                    <TextBlock Text="Дані профілю" Style="{StaticResource SectionTitle}"/>
                                    <TextBlock Style="{StaticResource SectionDescription}" Text="Оновіть свою особисту інформацію."/>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Ім'я та Прізвище" Style="{StaticResource FieldLabel}"/>
                                        </StackPanel>
                                        <Grid Grid.Column="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="FirstNameTextBox" Grid.Column="0" Style="{StaticResource SettingsTextBox}" Margin="0,0,5,0"/>
                                            <TextBox x:Name="LastNameTextBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Margin="5,0,0,0"/>
                                        </Grid>
                                    </Grid>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Ім'я користувача" Style="{StaticResource FieldLabel}"/>
                                            <TextBlock Text="Це ваше унікальне ім'я для входу." Style="{StaticResource FieldDescription}"/>
                                        </StackPanel>
                                        <TextBox Grid.Column="1" x:Name="UsernameTextBox" Style="{StaticResource SettingsTextBox}"/>
                                    </Grid>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                                            <TextBlock Text="Використовується для важливих повідомлень." Style="{StaticResource FieldDescription}"/>
                                        </StackPanel>
                                        <TextBox Grid.Column="1" x:Name="EmailTextBox" Style="{StaticResource SettingsTextBox}"/>
                                    </Grid>
                                </StackPanel>
                                <Border Style="{StaticResource CardFooter}" Margin="0,20,0,0">
                                    <Button Content="Зберегти профіль" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Margin="0" Click="SaveProfile_Click" Height="30" Width="150"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
                    <StackPanel MaxWidth="900">
                        <Grid Style="{StaticResource SettingsCard}">
                            <StackPanel>
                                <StackPanel Margin="25,25,25,0">
                                    <TextBlock Text="Зміна паролю" Style="{StaticResource SectionTitle}"/>
                                    <TextBlock Style="{StaticResource SectionDescription}" Text="Для зміни паролю, будь ласка, введіть ваш старий та новий пароль."/>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Старий пароль" Style="{StaticResource FieldLabel}"/>
                                        </StackPanel>
                                        <PasswordBox Grid.Column="1" x:Name="OldPasswordBox" Style="{StaticResource SettingsPasswordBox}"/>
                                    </Grid>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Новий пароль" Style="{StaticResource FieldLabel}"/>
                                            <TextBlock Text="Має бути мінімум 6 символів." Style="{StaticResource FieldDescription}"/>
                                        </StackPanel>
                                        <PasswordBox Grid.Column="1" x:Name="NewPasswordBox" Style="{StaticResource SettingsPasswordBox}"/>
                                    </Grid>
                                    <Grid Style="{StaticResource SettingsFormField}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Підтвердіть пароль" Style="{StaticResource FieldLabel}"/>
                                        </StackPanel>
                                        <PasswordBox Grid.Column="1" x:Name="ConfirmNewPasswordBox" Style="{StaticResource SettingsPasswordBox}"/>
                                    </Grid>
                                </StackPanel>
                                <Border Style="{StaticResource CardFooter}" Margin="0,20,0,0">
                                    <Button Content="Змінити пароль" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Margin="0" Click="ChangePassword_Click" Height="30" Width="150"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
                    <StackPanel MaxWidth="900">
                        <Grid Style="{StaticResource SettingsCard}">
                            <StackPanel>
                                <StackPanel Margin="25,25,25,0">
                                    <TextBlock Text="Додатково" Style="{StaticResource SectionTitle}"/>
                                    <TextBlock Style="{StaticResource SectionDescription}" Text="Юридична інформація та політики конфіденційності."/>
                                </StackPanel>

                                <StackPanel Margin="25,0,25,25">
                                    <TextBlock Style="{StaticResource FieldLabel}" FontSize="16" Margin="0,15,0,5">
                                        <Hyperlink NavigateUri="https://farmsteadhelper.pythonanywhere.com/static/terms_of_use.pdf"
                                                   RequestNavigate="Hyperlink_RequestNavigate">
                                            Умови Використання
                                        </Hyperlink>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource FieldDescription}" Text="Натисніть, щоб відкрити Умови Використання у вашому браузері."/>

                                    <TextBlock Style="{StaticResource FieldLabel}" FontSize="16" Margin="0,20,0,5">
                                        <Hyperlink NavigateUri="https://farmsteadhelper.pythonanywhere.com/static/privacy_policy.pdf"
                                                   RequestNavigate="Hyperlink_RequestNavigate">
                                            Політика Конфіденційності
                                        </Hyperlink>
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource FieldDescription}" Text="Натисніть, щоб відкрити Політику Конфіденційності у вашому браузері."/>
                                </StackPanel>

                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="40">
                    <StackPanel MaxWidth="900">
                        <Grid Style="{StaticResource SettingsCard}">
                            <StackPanel>
                                <StackPanel Margin="25,25,25,0">
                                    <TextBlock Text="Видалення профілю" Style="{StaticResource SectionTitle}" Foreground="{StaticResource DangerBrush}"/>
                                    <TextBlock Style="{StaticResource SectionDescription}" MaxWidth="600" HorizontalAlignment="Left">
                                        Ця дія є <Bold>незворотною</Bold>. Всі ваші дані, включно з усіма створеними мапами, будуть видалені назавжди.
                                        <LineBreak/><LineBreak/>
                                        Щоб підтвердити, будь ласка, перейдіть у розділ <Bold>Безпека</Bold> та введіть ваш поточний пароль у поле 'Старий пароль', після чого натисніть кнопку нижче.
                                    </TextBlock>
                                </StackPanel>
                                <Border Style="{StaticResource CardFooter}" Margin="0,20,0,0">
                                    <Button Content="Видалити акаунт" Style="{StaticResource DangerButton}" HorizontalAlignment="Right" Margin="0" Click="DeleteAccount_Click" Height="30" Width="150"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>